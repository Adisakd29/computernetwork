{
  "name": "netlab",
  "version": "1.0.0",
  "description": "NetLab virtual lab for computer networking class (vocational level)",
  "private": true,
  "scripts": {
    "build": "node scripts/fetch-assets.js",
    "prestart": "node scripts/fetch-assets.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "v86": "0.5.462"
  }
}
